{"name":"Unpack project","on":{"push":{"branches":["main"]}},"permissions":{"contents":"write"},"jobs":{"unpack":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"name":"Unpack and commit","run":"[ -f jockeyfinder.zip ] || exit 0; set -e; mkdir -p /tmp/un; unzip -oq jockeyfinder.zip -d /tmp/un; find . -mindepth 1 -maxdepth 1 ! -name .git ! -name .github -exec rm -rf {} +; rsync -a /tmp/un/jockeyfinder/ ./; git config user.name jockeyfinder-bot; git config user.email actions@users.noreply.github.com; git add -A; git commit -m deploy-jockeyfinder; git push"}]}}}
